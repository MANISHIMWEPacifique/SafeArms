// SafeArms Theme Configuration
// Centralized color palette and text styles

import 'package:flutter/material.dart';

class SafeArmsColors {
  // Background colors
  static const Color background = Color(0xFF1A1F2E);
  static const Color sidebarBackground = Color(0xFF252A3A);
  static const Color cardBackground = Color(0xFF2A3040);

  // Border colors
  static const Color border = Color(0xFF37404F);

  // Primary & accent
  static const Color primary = Color(0xFF1E88E5);
  static const Color primaryLight = Color(0xFF42A5F5);
  static const Color accent = Color(0xFF5C9CE6);

  // Status colors
  static const Color success = Color(0xFF3CCB7F);
  static const Color warning = Color(0xFFFFC857);
  static const Color error = Color(0xFFE85C5C);
  static const Color info = Color(0xFF42A5F5);

  // Text colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFFB0BEC5);
  static const Color textMuted = Color(0xFF78909C);
  static const Color textDisabled = Color(0xFF546E7A);

  // Status indicator colors
  static const Color active = Color(0xFF3CCB7F);
  static const Color inactive = Color(0xFF78909C);
  static const Color pending = Color(0xFFFFC857);
  static const Color rejected = Color(0xFFE85C5C);
}
