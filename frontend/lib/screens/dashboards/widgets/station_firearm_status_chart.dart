import 'package:flutter/material.dart';

class FirearmStatusChart extends StatelessWidget {
  const FirearmStatusChart({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
